-- All palette fixtures for the readability harness.
-- Each entry is a `term_colors`-shaped table matching what
-- ghosttysync.colors.ghosttyconfig.get_theme_info() would return.

return {
  require("tests.fixtures.palettes.rose-pine-moon"),
  require("tests.fixtures.palettes.material-design-colors"),
  require("tests.fixtures.palettes.aardvark-blue"),
  require("tests.fixtures.palettes.alabaster"),
  require("tests.fixtures.palettes.apple-system-light"),
  require("tests.fixtures.palettes.adventure-time"),
  require("tests.fixtures.palettes.arcoiris"),
  require("tests.fixtures.palettes.3024-day"),
}
